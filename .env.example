# Database
DATABASE_URL=postgresql://dev:dev@localhost:5433/dev

# Redis
REDIS_URL=redis://localhost:6379

# Authentik - Generate with: openssl rand -base64 32
AUTHENTIK_SECRET_KEY=change-me-in-production-min-32-chars-long
AUTHENTIK_BOOTSTRAP_PASSWORD=admin
AUTHENTIK_BOOTSTRAP_EMAIL=admin@localhost

# Authentik OAuth (Configure after setup)
AUTHENTIK_CLIENT_ID=
AUTHENTIK_CLIENT_SECRET=
AUTHENTIK_ISSUER=http://localhost:9000/application/o/tfwx-app/

# API
PORT=3001
NODE_ENV=development

# Frontend
VITE_API_URL=http://localhost:3001
VITE_AUTHENTIK_URL=http://localhost:9000